{
  "name": "@checkdigit/github-actions",
  "version": "4.0.4",
  "description": " Provides supporting operations for github action builds.",
  "homepage": "https://github.com/checkdigit/github-actions#readme",
  "bugs": {
    "url": "https://github.com/checkdigit/github-actions/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/checkdigit/github-actions.git"
  },
  "license": "MIT",
  "author": "Check Digit, LLC",
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "build:dist-mjs": "node -e \"require('node:fs').rmSync('dist-mjs', { recursive: true, force: true })\" && npx builder --type=module --sourceMap --outDir=dist-mjs",
    "ci:compile": "tsc --noEmit",
    "ci:coverage": "node -e \"const fs = require('node:fs'); fs.rmSync('coverage', { recursive: true, force: true }); fs.mkdirSync('coverage')\" && node --experimental-test-module-mocks --disable-warning ExperimentalWarning --test-timeout 600000 --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter spec --test-reporter-destination stdout --test \"src/**/*.spec.ts\"",
    "ci:lint": "npm run lint",
    "ci:style": "npm run prettier",
    "ci:test": "node --experimental-test-module-mocks --disable-warning ExperimentalWarning --pending-deprecation --throw-deprecation --test-timeout 600000 --test \"src/**/*.spec.ts\"",
    "coverage": "node --experimental-test-module-mocks --disable-warning ExperimentalWarning --test-timeout 600000 --experimental-test-coverage --test \"src/**/*.spec.ts\"",
    "lint": "eslint --max-warnings 0 .",
    "lint:fix": "eslint . --fix",
    "prettier": "prettier . --check",
    "prettier:fix": "prettier . --write",
    "test": "npm run ci:compile && npm run ci:test && npm run ci:lint && npm run ci:style"
  },
  "prettier": "@checkdigit/prettier-config",
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1",
    "@checkdigit/time": "^6.0.0",
    "@octokit/rest": "^22.0.1",
    "debug": "^4.4.3",
    "semver": "^7.8.5"
  },
  "devDependencies": {
    "@checkdigit/eslint-config": "^12.1.0",
    "@checkdigit/prettier-config": "^8.1.1",
    "@checkdigit/typescript-config": "^10.2.1",
    "@types/debug": "^4.1.13",
    "@types/semver": "^7.8.0",
    "nock": "^14.0.17"
  },
  "engines": {
    "node": ">=24.13"
  },
  "allowScripts": {
    "esbuild@0.28.2": false,
    "unrs-resolver@1.12.2": false
  }
}
